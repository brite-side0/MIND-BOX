# Dependencies
node_modules/

# Builds
dist/
web/stats.html
target/
*.wasm

# Bundle analysis report (generated locally by `pnpm --filter @mindbox/web bundle:stats`)
web/stats.html

# Secrets — never commit real env files (only .env.example is tracked)
.env
.env.local
.env.*.local

# Stellar CLI local identities (contain secret keys)
.stellar/

packages/registry-client/bindings-tmp/

# Logs
*.log

# Editor / OS
.DS_Store
.vercel
.idea/

# pnpm content-addressable store (never commit)
.pnpm-store/
